{
  "scripts": {
    "dev": "npx server -l 3000",
    "build": "node -e \"console.log('No frontend build step required')\"",
    "start": "node server.js"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.2.2",
    "tailwindcss": "^4.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
